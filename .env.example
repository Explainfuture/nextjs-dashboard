# App
AUTH_SECRET=replace_with_random_secret
AUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true

# Database
POSTGRES_URL=postgres://user:password@host:6543/postgres?sslmode=require

# GitHub OAuth (Local)
AUTH_GITHUB_ID=replace_with_local_github_client_id
AUTH_GITHUB_SECRET=replace_with_local_github_client_secret

# Google OAuth (Optional)
AUTH_GOOGLE_ID=replace_with_google_client_id
AUTH_GOOGLE_SECRET=replace_with_google_client_secret

# Mail (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx
AUTH_EMAIL_FROM=Acme <noreply@your-domain.com>

# Public App URL for links in emails
NEXT_PUBLIC_APP_URL=http://localhost:3000
